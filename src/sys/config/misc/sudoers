# /etc/sudoers
#
# This file MUST be edited with the 'visudo' command as root.
#
# See the man page for details on how to write a sudoers file.
#

Defaults	env_reset,tty_tickets

Defaults:ALL    timestamp_timeout=0


# Host alias specification


#No longer restrict commmand execution by host because I set it later in system configuration and it would be a pain in the back to rearrange it. External access is already restricted by several measures. No need for an extra one.
#Host_Alias      LOCH = ubuntu


# User alias specification

# Cmnd alias specification

# User privilege specification

#Root has all privileges
root	ALL=(ALL) ALL

#User can only execute the scripts containing the privileged operations
vtuji   ALL=(root) NOPASSWD:/usr/local/bin/privileged-ops.sh,/usr/local/bin/privileged-setup.sh


# (Note that later entries override this, so you might need to move
# it further down)

